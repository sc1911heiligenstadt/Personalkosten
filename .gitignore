# Startbestand/Seed enthält echte Personendaten -> niemals committen (nur Cloud)
*.seed.json
__seed_test.json
seed*.json

# Lokales Claude-Projektgedaechtnis, nicht Teil des oeffentlichen Repos
CLAUDE.md
